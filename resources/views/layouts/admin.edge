<!DOCTYPE html>
<html :class="{ 'theme-dark': theme.dark }" x-data="data()" lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  
  @entryPointStyles('admin')
  @entryPointScripts('admin')
</head>

<body>
  <div class="flex h-screen bg-gray-50 dark:bg-gray-900" :class="{ 'overflow-hidden': sidebar.open}">
    @include('admin/partials/sidebar/structure')
    
    <div class="flex flex-col flex-1">
      @include('admin/partials/header')

      <main class="h-full pb-16 overflow-y-auto">
        @include('admin/partials/alerts')
        @!section('body')
      </main>
    </div>
  </div>
</body>

</html>